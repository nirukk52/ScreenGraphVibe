/**
 * @module features/app-launch-config/port
 * @description Feature-scoped port to depend on instead of direct :data usage. Commented for now.
 */

// export interface AppLaunchConfigDataPort {
//   getAll(): Promise<unknown[]>;
//   getById(id: string): Promise<unknown | null>;
//   create(input: unknown): Promise<unknown>;
//   update(id: string, input: unknown): Promise<unknown>;
//   remove(id: string): Promise<boolean>;
//   getDefault(): Promise<unknown | null>;
//   setDefault(id: string): Promise<unknown>;
// }


