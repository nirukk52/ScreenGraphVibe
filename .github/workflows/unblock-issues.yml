name: Unblock Dependent Issues

on:
  pull_request:
    types: [closed]
  issues:
    types: [closed]

permissions:
  issues: write
  pull-requests: read

jobs:
  unblock:
    if: github.event.pull_request.merged == true || github.event.issue.state == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Unblock dependent issues using GraphQL
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.SG_BOT_TOKEN }}
          script: |
            // Get closed issue numbers
            const closedIssues = [];
            
            if (context.payload.pull_request) {
              const prBody = context.payload.pull_request.body || '';
              const matches = [...prBody.matchAll(/(?:closes|resolves|fixes)\s+#(\d+)/gi)];
              closedIssues.push(...matches.map(m => parseInt(m[1])));
            } else if (context.payload.issue) {
              closedIssues.push(context.payload.issue.number);
            }
            
            console.log(`🔍 Closed issues: ${closedIssues.join(', ')}`);
            
            if (closedIssues.length === 0) {
              console.log('⚠️ No closed issues found');
              return;
            }
            
            // Query closed issue for native relationships (trackedIssues) and timeline ConnectedEvent fallback
            const getClosedIssueRelationships = `
              query($owner: String!, $repo: String!, $issueNumber: Int!) {
                repository(owner: $owner, name: $repo) {
                  issue(number: $issueNumber) {
                    number
                    trackedIssues(first: 100) {
                      totalCount
                      nodes { number state }
                    }
                    trackedInIssues(first: 100) {
                      totalCount
                      nodes { number state }
                    }
                    timelineItems(itemTypes: CONNECTED_EVENT, last: 100) {
                      nodes {
                        __typename
                        ... on ConnectedEvent {
                          connectionType
                          subject { __typename ... on Issue { number state } }
                          source { __typename ... on Issue { number state } }
                        }
                      }
                    }
                  }
                }
              }
            `;

            // Query details for a candidate blocked issue
            const getIssueDetails = `
              query($owner: String!, $repo: String!, $issueNumber: Int!) {
                repository(owner: $owner, name: $repo) {
                  issue(number: $issueNumber) {
                    number
                    state
                    title
                    url
                    assignees(first: 10) { nodes { login } }
                    labels(first: 20) { nodes { name } }
                    trackedInIssues(first: 100) { nodes { number state } }
                  }
                }
              }
            `;
            
            // Process each closed issue
            for (const closedNum of closedIssues) {
              console.log(`\n🔍 Processing closed issue #${closedNum}`);
              
              try {
                // Query this specific closed issue to find what it blocks (native + timeline fallback)
                const result = await github.graphql(getClosedIssueRelationships, {
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issueNumber: closedNum
                });
                
                const closedIssue = result.repository.issue;
                if (!closedIssue) {
                  console.log(`   ⚠️ Issue #${closedNum} not found`);
                  continue;
                }
                // Gather blocked issues from native trackedIssues
                const nativeBlocked = (closedIssue.trackedIssues?.nodes || []).map(n => n.number);
                const nativeCount = closedIssue.trackedIssues?.totalCount ?? 0;

                // Fallback: derive blocked issues from timeline ConnectedEvent where connectionType == BLOCKS
                const timelineNodes = closedIssue.timelineItems?.nodes || [];
                const timelineBlocked = timelineNodes
                  .filter(n => n.__typename === 'ConnectedEvent' && n.connectionType === 'BLOCKS' && n.source?.__typename === 'Issue' && n.source.number === closedNum && n.subject?.__typename === 'Issue')
                  .map(n => n.subject.number);

                // Compose unique candidate blocked issue numbers
                const candidateBlockedNumbers = Array.from(new Set([...nativeBlocked, ...timelineBlocked]));

                if (candidateBlockedNumbers.length === 0) {
                  console.log(`   ℹ️ Issue #${closedNum} doesn't block any other issues (native=${nativeCount}, timeline=${timelineBlocked.length})`);
                  continue;
                }

                console.log(`   🔗 Candidates blocked by #${closedNum}: ${candidateBlockedNumbers.join(', ')} (native=${nativeCount}, timeline=${timelineBlocked.length})`);

                // For each candidate, fetch details and decide unblocking
                for (const candidate of candidateBlockedNumbers) {
                  const details = await github.graphql(getIssueDetails, {
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    issueNumber: candidate
                  });

                  const issue = details.repository.issue;
                  if (!issue) continue;
                  if (issue.state !== 'OPEN') {
                    console.log(`   ⏭️ Issue #${issue.number} already ${issue.state.toLowerCase()}, skipping`);
                    continue;
                  }

                  const blockers = issue.trackedInIssues?.nodes || [];
                  const blockerNumbers = blockers.map(b => b.number);
                  const allResolved = blockers.every(b => b.state === 'CLOSED');
                  const stillBlocked = blockers.filter(b => b.state !== 'CLOSED');

                  if (allResolved) {
                    console.log(`   ✅ Issue #${issue.number} is now UNBLOCKED (blockers: ${blockerNumbers.join(', ')})`);
                    const assignees = (issue.assignees?.nodes || []).map(a => `@${a.login}`).join(' ');
                    await github.rest.issues.createComment({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      issue_number: issue.number,
                      body: `✅ **Unblocked!** All dependencies resolved (${blockerNumbers.map(n => `#${n}`).join(', ')}).\n\n${assignees || '@' + context.repo.owner} This issue is ready to work on.`
                    });
                    const currentLabels = (issue.labels?.nodes || []).map(l => l.name);
                    const newLabels = currentLabels.filter(l => l !== 'status: blocked');
                    if (!newLabels.includes('status: ready')) newLabels.push('status: ready');
                    await github.rest.issues.setLabels({ owner: context.repo.owner, repo: context.repo.repo, issue_number: issue.number, labels: newLabels });
                  } else {
                    console.log(`   ⏸️ Issue #${issue.number} still blocked by: ${stillBlocked.map(b => `#${b.number}`).join(', ')}`);
                    await github.rest.issues.createComment({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      issue_number: issue.number,
                      body: `⏸️ Partially unblocked: #${closedNum} resolved.\n\nStill waiting on: ${stillBlocked.map(b => `#${b.number}`).join(', ')}`
                    });
                  }
                }
              } catch (error) {
                console.error(`   ❌ Error processing issue #${closedNum}: ${error.message}`);
              }
            }
            
            console.log('\n✅ Dependency check complete');
