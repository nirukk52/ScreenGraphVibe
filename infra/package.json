{
  "name": "@screengraph/infra",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "deploy": "node dist/deploy.js",
    "test": "vitest run",
    "test:unit": "echo 'No unit tests found for infra' || vitest run src/**/*.test.ts",
    "test:integration": "echo 'No integration tests found for infra' || vitest run src/**/*.integration.test.ts",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "zod": "^3.22.4",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.1.0"
  }
}
